# Contributing

Create a focused branch, keep tenant boundaries and permissions explicit, add tests for behavioral changes, and never commit credentials or customer data. Run backend tests/Pint and frontend type-check/lint/tests/build before opening a pull request. Migrations must be forward-safe and must not destroy production data. Use conventional, descriptive commit messages and document API changes in `docs/API.md`.
